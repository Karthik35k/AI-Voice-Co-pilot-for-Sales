# LLM API Key (Google Gemini, OpenAI, or compatible provider)
LLM_API_KEY=

# Optional: Provider choice ('gemini', 'openai', or 'mock')
# Defaults to auto-detecting based on API Key format, or fallback mock provider if empty.
LLM_PROVIDER=gemini

# Express Server Port
PORT=3001
